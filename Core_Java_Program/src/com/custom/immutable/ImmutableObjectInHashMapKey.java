package com.custom.immutable;

/* We are using Immutable class(final class) object in HashMap key because of String class is immutable and, all Wrapper class object like Integer, Float, Double are immutable by default. So we can use immutable class in HashMap key*/
public class ImmutableObjectInHashMapKey {

	public static void main(String[] args) {
		

	}

}
